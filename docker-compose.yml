services:
  miyan-frontend:
    build:
      context: .
    container_name: miyan-frontend
    restart: unless-stopped
    ports:
      - "${PORT:-3000}:3000"
    env_file:
      - .env
    environment:
      HOST: 0.0.0.0
      PORT: 3000
    command: ["node", ".output/server/index.mjs"]
